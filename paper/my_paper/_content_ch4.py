"""第4章 关键技术实现（重点：WebSocket + 加密）"""
import os
from _gen_paper import add_para, add_heading, add_image, add_code_block, add_page_break, DIA


def write_ch4(doc):
    add_heading(doc, '4  关键技术实现', level=1)
    add_para(doc, '本章按实现链路往下展开。'
                  '4.1 节先交代用户认证与 JWT，'
                  '4.2 节说明好友关系如何落库和校验，'
                  '4.3 节进入 WebSocket 实时聊天，'
                  '4.4 节再把重点放到端到端加密上。真正能拉开系统差异的，主要还是后两节。')

    add_para(doc, '要先说明一下：4.1 与 4.2 节走的是相对常规的实现路径，'
                  '更多是把系统的基础设施搭起来；'
                  '真正能体现本文价值的，'
                  '是 4.3 节里 WebSocket 的连接管理，以及 4.4 节那套端到端加密的设计取舍。')

    add_heading(doc, '4.1  用户认证与 JWT 实现', level=2)
    add_para(doc, '用户认证算是整个系统的入口——'
                  '后续所有业务都建立在"这条请求来自哪个用户"这件事上。'
                  '后端把注册和登录拆成 AuthService 里的两个独立方法。'
                  '密码不能明文存储，这是底线；'
                  '本系统选 bcrypt 是因为它内置盐值，'
                  '并且允许调节工作因子，'
                  '离线穷举的成本会随着硬件升级一起水涨船高——'
                  '比裸的 MD5 或 SHA1 稳健得多。')
    add_para(doc, '登录通过后，服务端用 HMAC-SHA256 签发一个 JWT。'
                  'Payload 里塞着用户 ID 和过期时间戳，'
                  '签名密钥只保留在服务端配置里，绝不下发。'
                  '前端拿到 Token 之后存进 localStorage，'
                  'HTTP 请求里走 "Authorization: Bearer <token>" 头，'
                  'WebSocket 握手则改走 URL 查询参数——'
                  '两条路径同一份令牌，验签逻辑完全复用。')
    add_para(doc, '受保护的接口在鉴权这件事上没有让每个 Handler 自己写一份逻辑，而是统一交给 AuthMiddleware 处理。'
                  '中间件每次从请求头里把 JWT 取出来，再用同一把密钥去验签——遇到过期、伪造、根本就没带 token 这三种情形，'
                  '都直接以 401 把请求挡回去。验签过了的话就把用户 ID 放进 gin.Context，业务 Handler 那边只需 c.Get("userId") 取出来用就行。'
                  '把鉴权集中到一道关卡上之后的好处很直接：业务 Handler 里几乎看不到任何跟鉴权有关的样板代码。')
    add_para(doc, 'bcrypt 工作因子，本系统取的是默认值 10，'
                  '一次哈希大约 60-80 ms。'
                  '这个量级对登录接口完全可接受，'
                  '但对暴力破解者来说意味着每秒只能尝试十几次，'
                  '相比 MD5 那种数十亿次/秒的速度，几乎是压倒性差距。'
                  '万一 GPU 算力又上一个台阶，'
                  '只要把工作因子改成 11 甚至 12 重新加密一遍，'
                  '系统的防护强度就能跟上来。')

    add_para(doc, '关键中间件代码如下：')
    add_code_block(doc, '''func AuthMiddleware() gin.HandlerFunc {
    return func(c *gin.Context) {
        tokenStr := strings.TrimPrefix(c.GetHeader("Authorization"), "Bearer ")
        claims, err := ParseJWT(tokenStr)
        if err != nil {
            c.AbortWithStatusJSON(401, gin.H{"message": "未授权"})
            return
        }
        c.Set("userId", claims.UserID)
        c.Next()
    }
}''')

    add_heading(doc, '4.2  好友关系处理', level=2)
    add_para(doc, '好友关系由两张表协同支撑——'
                  'friend_requests 记录申请，friends 记录已建立的关系。'
                  '业务流程不复杂，但有几个细节需要拿捏。')
    add_para(doc, '用户 A 给 B 发申请时，后端要先做三道前置校验：'
                  'B 是否存在、是否已经是好友、是否还有未处理的旧申请。'
                  '任何一道没过，请求直接被驳回；'
                  '都过了，才在 friend_requests 表里写一条 status=待处理 的记录。'
                  '这一步把"重复申请"挡在数据库之外，省得后面再靠唯一索引兜底。')
    add_para(doc, 'B 在申请页处理时——同意或拒绝——会触发不同分支。'
                  '同意路径走数据库事务，'
                  '把 friend_requests 的 status 改为"已同意"的同时，'
                  '向 friends 表插入 (A, B) 和 (B, A) 两条记录。'
                  '为什么是两条？因为这样 A 查好友能看到 B、B 查好友能看到 A，'
                  '查询路径上完全对称，业务代码不需要做"双向 OR"判断，逻辑更清爽。'
                  '拒绝路径则只更新状态，friends 表保持不动。')
    add_para(doc, '聊天与历史消息接口在执行任何业务之前，'
                  '都会先调用 FriendService.IsFriend(senderId, receiverId)，'
                  '把"非好友越权发消息"这条攻击面直接堵死。'
                  '把校验放在 Service 层而不是 Handler 层，'
                  '是为了让 HTTP 和 WebSocket 两条入口都强制走同一道闸门。')

    # ========== 4.3 WebSocket 重点 ==========
    add_heading(doc, '4.3  基于 WebSocket 的实时聊天实现', level=2)
    add_para(doc, '实时聊天是这套系统里最见工程功夫的一部分。'
                  '轮询和长轮询并非完全不能用，但放到 IM 场景里，低延迟、持续在线、服务端主动下行这几件事会很快把它们的短板暴露出来。'
                  'WebSocket 更贴近这里真正需要的通信方式：客户端不必反复发起请求，服务端也能主动把新消息推到目标连接。'
                  '下面不按抽象原理讲，而是顺着实现路径往下拆：先看连接怎么建，再看连接怎么管，之后处理异常，最后把整条消息链路串起来。')

    add_heading(doc, '4.3.1  WebSocket 连接建立与鉴权', level=3)
    add_para(doc, 'WebSocket 在 RFC 6455 里复用 HTTP 完成握手——'
                  '客户端发一个带 "Upgrade: websocket" 头的 GET，'
                  '服务端校验后返回 101 完成协议切换。'
                  '握手本质上还是一次 HTTP 请求，'
                  '这就给鉴权留下了天然的入口：'
                  '本系统选择把 JWT 通过 URL 查询参数 token=<jwt> 传给服务端，'
                  '在握手阶段就完成验证。')
    add_para(doc, '另一种常见做法是"先放行连接、再让客户端首帧发 token"。'
                  '但这样做有两个问题：'
                  '未鉴权连接会提前占用服务端资源；'
                  '逻辑上要在 WS 层再绕一层"伪鉴权握手"，反而更复杂。'
                  '把校验前移到握手阶段更直接，'
                  '代价是 token 会出现在 URL 里——'
                  '反向代理和访问日志可能都会记下来，'
                  '所以 token 的过期时间要短，敏感字段更不要往 Payload 里塞。')
    add_para(doc, 'WebSocket 升级处理的关键代码如下：')
    add_code_block(doc, '''func (h *WSHandler) Serve(c *gin.Context) {
    token := c.Query("token")
    claims, err := ParseJWT(token)
    if err != nil {
        c.AbortWithStatusJSON(401, gin.H{"message": "未授权"})
        return
    }
    conn, err := upgrader.Upgrade(c.Writer, c.Request, nil)
    if err != nil {
        return
    }
    h.hub.Register(claims.UserID, conn)
    defer h.hub.Unregister(claims.UserID, conn)
    h.readLoop(claims.UserID, conn) // 阻塞读循环，连接断开后退出
}''')

    add_para(doc, '这里有个细节容易被忽视——WebSocket 虽然最后是要升级成长连接的，但它的入口形式仍然是一条标准 HTTP 请求，'
                  '因此一样要从 Gin 的中间件链路上过一遍。CORS 响应头、统一日志这种横切关心可以直接拿来用。'
                  '但鉴权这一块就不能照搬普通 HTTP 接口的做法了：JWT 在 HTTP 那边一般是放在 Authorization 头里走，'
                  'WebSocket 握手则更适合直接从 URL 的查询参数里取 token。要是硬塞一套中间件去同时兼容两种来源，最后多半是来源冲突、'
                  '责任边界一团乱。本系统的解法是把 /ws 单拎出来挂成一组独立路由，JWT 验签这件事干脆放在 WS 自己的 Handler 里直接做完。'
                  '这种安排实际上是把"协议升级"和"身份确认"绑在同一个入口里搞定，避免没鉴权的连接顺势溜进 Hub。')

    add_heading(doc, '4.3.2  连接管理与消息广播', level=3)
    add_para(doc, 'WebSocket Hub 在实时聊天这条链路里相当于"调度中心"的角色。它的内部其实没什么复杂结构，'
                  '核心就是一张加了并发保护的 map——key 用用户 ID，value 是这位用户当前所有活跃连接组成的列表。'
                  '新连接进来就追加进对应用户的列表里，断开就把那一条摘掉；需要推送消息时，从列表里挨条把数据写出去。'
                  '允许一个用户挂多条连接是有意这么设计的，多标签页或者多设备同时在线这种场景就被天然兼容了，业务侧不需要为这事再写额外逻辑。')
    add_para(doc, '连接管理上真正能拉开工程差距的地方，集中在并发控制和广播路径怎么写。'
                  '本系统的处理方式可以叫"短临界区 + 锁外写"——先在读写锁的保护下把目标用户的连接列表整体复制一份出来，'
                  '锁一释放，再去逐条调用 conn.WriteMessage。这样写不是为了好看，是被网络 I/O 的特性逼出来的：'
                  '它的耗时跟内存操作根本不是一个量级，万一在持锁的状态下直接往 Socket 上灌数据，'
                  '只要里面有一个慢客户端，整个 Hub 的广播路径就会被它一个人拖死，锁竞争还会跟着放大。'
                  '换种说法，这种设计本质是在"严格一致"和"整体吞吐"之间偏向后者——'
                  '允许那张连接列表在毫秒级时间窗里短暂跟真实状态有出入，换来广播链路在最差客户端面前依然稳得住。'
                  '至于写失败的连接，系统会主动从列表里摘掉、把底层 TCP 关掉，避免无效连接长期占内存。')
    add_para(doc, '为什么要允许一个用户挂多条连接？'
                  '这是 IM 系统里一个常见但容易被忽略的细节。'
                  '现实里用户完全可能同时挂在公司电脑、家里的笔记本和手机网页上——'
                  '如果 Hub 强行只保留最新一条，'
                  '前面那些客户端就会出现"假装在线但收不到消息"的情况。'
                  '本系统选择追加而不是覆盖，'
                  '把所有活跃连接都纳入推送范围；'
                  '代价是同一条消息要给同一用户多写几次，'
                  '但和"悄无声息的消息丢失"相比，这点开销可以接受。')

    add_para(doc, 'Hub 在某位用户那找不到活跃连接的时候，会把这个人视作离线。要紧的不是"消息能不能立刻到"，'
                  '而是"消息最终一定能到"。系统把"消息存储"和"消息实时投递"刻意拆成了两层——'
                  'HTTP 那条管把消息写到数据库里，把持久化的可靠性兜住；WebSocket 这条只管在用户在线时把数据低延迟推过去。'
                  '所以哪怕接收方此刻不在线、或者推送中途失败、或者网络突然断了，消息本身都依然完整地躺在 messages 表里。'
                  '等用户再次打开聊天窗口时，前端会通过 GET /api/messages?friendId=xxx 把历史密文拉回去，再在本地解密展示。'
                  '这套做法事实上是放弃了"绝对实时必达"那种强承诺，但换回来的是一条更简单、更易于验证、丢消息风险也更低的实现路径。'
                  '在一篇毕业设计的尺度上，这种取舍是合适的。')

    add_heading(doc, '4.3.3  异常处理与重连机制', level=3)
    add_para(doc, '现实的网络远没有局域网那么稳定——'
                  '网络切换、Wi-Fi 抖动、服务端重启都可能让 WS 连接突然失效。'
                  '前端的 utils/websocket.ts 把连接创建逻辑统一封装起来，'
                  '异常处理分了三个层次。')
    add_para(doc, '第一层是状态可见。'
                  'ChatView.vue 维护一个 socketConnected 响应式变量，'
                  'onopen / onclose 各自更新它，'
                  '界面顶部据此显示"在线同步中"或"等待连接"——'
                  '让用户能直接看到当前的连接状态，而不是凭感觉猜。'
                  '第二层是错误反馈，'
                  'onerror 触发时把错误写入 errorMessage，'
                  '醒目地提示"WebSocket 连接失败"，'
                  '避免用户在断网状态下还在反复点发送。'
                  '第三层是资源回收，'
                  '在 onBeforeUnmount 钩子里主动 socket.close()，'
                  '防止 Hub 残留无效连接占用内存。')
    add_para(doc, '至于自动重连，本系统刻意没做复杂的指数退避机制。'
                  '原因前面已经提过——消息已经持久化在数据库里，'
                  'WS 只负责实时推送这一件事。'
                  '就算连接短暂断开，用户刷新页面或重新进入聊天，'
                  '一次 GET /api/messages 就能把历史拉回来。'
                  '把可靠性交给数据库、把实时性交给 WS，这种解耦既简单又有效。')

    add_heading(doc, '4.3.4  消息收发完整时序', level=3)
    add_para(doc, '把前面几节的设计拼起来，'
                  '一条消息从 A 的浏览器走到 B 的浏览器，'
                  '完整时序如图 4-1 所示。'
                  '图里有一处细节希望读者留意——'
                  '服务端这条泳道从头到尾只接触密文，'
                  '明文只存在于两端浏览器里。')
    add_image(doc, os.path.join(DIA, '03_WebSocket聊天时序图.png'),
              '图 4-1  基于 WebSocket 的实时聊天时序图', width_inch=6.2)
    add_para(doc, 'A 在聊天页输入内容、点击发送的瞬间，'
                  '前端先调用 e2ee.ts 里的加密函数，'
                  '用 A 的公钥和 B 的公钥分别对同一段明文加密一次，'
                  '得到 sender_ciphertext 和 receiver_ciphertext 两份密文。'
                  '随后通过 POST /api/messages 把两份密文连同接收方 ID 一并提交。')
    add_para(doc, '请求到了后端 MessageService 之后，会先过三道校验：双方现在是不是好友关系、两份密文字段是不是都合法、'
                  '请求里带的算法标识是不是落在 rsa-oaep-sha256 白名单内。任意一道没过就直接被拒；'
                  '全部通过才允许把两份密文真正写到 messages 表里。'
                  '写库成功之后做两件事——一是经由 WebSocket Hub 给 B 那一端推一条 chat_message 类型的 JSON 帧，'
                  '内容包含这条消息的 ID、发送方 ID，以及密文和算法标识；二是给 A 返一个 HTTP 201，前端拿到响应后把这条消息追加到自己的本地列表上。')
    add_para(doc, 'B 端收到 WS 帧之后，'
                  'selectMessagePayloadForUser 会自动挑出 receiver_ciphertext，'
                  '用本地私钥解开并渲染。'
                  '如果解密失败——'
                  '比如对方在另一台设备生成了新密钥、本地拿到的还是老公钥——'
                  '界面会退化成"***（已加密）"的占位文本，'
                  '不会因为单条消息异常导致整个会话崩掉。')
    add_para(doc, '这条流程里 HTTP 和 WebSocket 各做各的事：'
                  'HTTP 保证消息可靠落库——是"持久化通道"；'
                  'WebSocket 负责实时推送——是"实时通道"。'
                  '消息不丢由数据库兜底，实时性由 WebSocket 推送保证，'
                  '两者并不互相重叠。')

    # ========== 4.4 加密 重点 ==========
    add_heading(doc, '4.4  端到端消息加密机制', level=2)
    add_para(doc, '端到端消息加密是本文最核心的技术亮点。'
                  '传统 IM 大多依赖 TLS 做链路保护，'
                  '这意味着消息在客户端和服务端之间传输时是加密的，'
                  '但一旦到达服务端，通常就以明文形式进入数据库或业务内存。'
                  '换个角度看，TLS 解决的是"中间节点能否窃听"这件事，'
                  '并不能解决"服务端本身是否天然可信"这件事。'
                  '本系统选择把安全边界往浏览器本地推：'
                  '明文在发送前就完成加密，服务端只接触密文，'
                  '消息内容从服务端的明文处理链路里被彻底剥离出来。'
                  '这种做法当然消除不了所有风险，'
                  '但它实质上把攻击面整个换了一种形态——'
                  '最常见的数据库泄露后果，'
                  '从"直接看到消息内容"被压成了"只能拿到不可读密文"。')

    add_heading(doc, '4.4.1  设计目标与总体思路', level=3)
    add_para(doc, '这套加密方案一开始就不是只盯着“能加密”三个字。'
                  '第一条底线很明确：服务端不能碰到消息明文，否则所谓端到端加密就只剩一个名号。'
                  '但如果只顾安全、不顾体验，方案同样站不住脚。发送方和接收方都必须能在本地回看历史消息，'
                  '不能出现“只有接收方能读、发送方自己反而看不到”的情况。再往下才是另外两个要求：'
                  '整个过程最好对用户透明，密钥生成、上传和加解密都放到背后完成；算法也必须采用公开、成熟、经过同行评议较多的非对称加密方案，而不是自己造轮子。')
    add_para(doc, '把这些约束摞在一起，整体路线就只剩一条比较窄的可走——浏览器侧生成密钥对、前端做双重加密、服务端只保留密文做存储和转发，'
                  '具体见图 4-2。这条路上没有去引入会话密钥协商，也没有上双棘轮这种更精细的密钥派生机制；先求把链路打通，'
                  '让加密的边界、密钥的流向、可验证的关键节点都看得清楚。换言之，本文并不是要拿一份毕业设计去比肩 Signal 那种工业协议，'
                  '而是想把 E2EE 那套核心想法——"明文从不离开浏览器"——以尽可能小的实现成本，真的塞进一个能跑起来的 Web 端 IM 里。')
    add_image(doc, os.path.join(DIA, '04_端到端加密流程图.png'),
              '图 4-2  端到端消息加密总流程图', width_inch=6.2)

    add_para(doc, '这套思路在密码学里其实非常老——往前追到 PGP 那个年代就是一回事：用接收方的公钥加密，用接收方的私钥解密。'
                  '本文要做的不是发明新的算法，而是回答另一个具体得多的问题——这套老办法怎么落进今天浏览器的运行环境里，'
                  '另外要顺手解决一个真实用户会问的细节，"我作为发送方，也想在自己设备上回看历史消息"。这些问题更像工程问题而不是数学问题。')
    add_para(doc, '有两条边界要先划清楚。'
                  '一是本方案处理的是消息级加密，与传输层无关——'
                  '前者保证服务端看不到明文，'
                  '后者保证中间节点看不到明文，'
                  '两者不冲突，工程上应当同时启用 TLS。'
                  '二是本方案不涉及密钥协商，'
                  '直接用每个用户的长期 RSA 密钥对做加解密——'
                  '简洁的代价是无法做到前向保密。'
                  '把这两条边界写明白，'
                  '是想避免后续读者把它误读成"完整的工业级 E2EE"。')

    add_heading(doc, '4.4.2  RSA-OAEP 密钥对生成与管理', level=3)
    add_para(doc, '密钥这一步整个交给浏览器自己来做，调用的是 Web Crypto API 暴露的 window.crypto.subtle.generateKey。'
                  '参数也没怎么折腾——算法 RSA-OAEP、模长 2048、公开指数取常用的 65537、哈希走 SHA-256。'
                  '为什么不沿用早期那种 RSA-PKCS#1 v1.5？答得也比较直接：OAEP 借助随机填充加 MGF，'
                  '对付选择密文攻击的能力比前者强不少，在现代 RSA 实现里已经成了更稳的默认选择。'
                  '另外要说一句的是 Web Crypto API 本身——它是浏览器原生的实现，不需要再拉一个第三方 JS 加密库进来；'
                  '性能、稳定性和接口一致性这几方面，都更适合塞到一条要长期运行的业务链路上。'
                  '密钥生成完之后，私钥按 PKCS#8 格式导出，公钥按 SPKI 格式导出，最后统一编成 Base64 字符串保存——'
                  '这样落到 localStorage 也好、走 HTTP 接口往服务端上传也好，都比较方便。')
    add_para(doc, '前端密钥生成与导出的关键代码如下：')
    add_code_block(doc, '''export async function generateKeyPair() {
  return window.crypto.subtle.generateKey(
    {
      name: 'RSA-OAEP',
      modulusLength: 2048,
      publicExponent: new Uint8Array([1, 0, 1]),
      hash: 'SHA-256'
    },
    true,
    ['encrypt', 'decrypt']
  )
}''')
    add_para(doc, '私钥落地的位置只有一个——用户当前这台设备的 localStorage 里，存储键格式是 e2ee-private-key:<userId>。'
                  '这条数据不会被任何接口往服务端发——"私钥不离开本机"这条规则是端到端加密的底线，'
                  '在工程上没人会主动替你保证，必须自己亲手守住。'
                  '用户第一次点进聊天功能时，前端会先看本地有没有现成的私钥：'
                  '没有就当场生成一对新的，再把公钥推到服务端；如果本地有、但服务端那把对应公钥却查不到'
                  '（最常见的情形是老用户上次清过浏览器缓存，私钥本身没掉只是被 localStorage 漂移了一下），'
                  '就从私钥反向算出公钥再补传一份过去。')

    add_para(doc, '为什么选 localStorage 而不是 sessionStorage 或 IndexedDB？'
                  'sessionStorage 在刷新或关闭浏览器后就清空了，'
                  '加密会话会随之中断，体验太差；'
                  'IndexedDB 接口繁琐，'
                  '而本系统要存的只是一条 1.6 KB 左右的 RSA 私钥——'
                  '杀鸡用牛刀。'
                  'localStorage 在简单与可用之间提供了一个恰到好处的平衡。'
                  '另一个细节是，存储键里专门带上了用户 ID，'
                  '同一台浏览器即便切换账号，私钥也不会互相覆盖。')

    add_heading(doc, '4.4.3  公钥上传与好友公钥获取', level=3)
    add_para(doc, '密钥对在浏览器里生成出来以后，前端会通过 PUT /api/users/me/public-key 把公钥连同算法标识 rsa-oaep-sha256 一起发到服务端，'
                  '由后端写到 users 表的 public_key 与 public_key_algorithm 这两列里。'
                  '到 A 真正要给 B 发消息的时候，B 那把公钥不需要单独发请求去查——它早就在 A 调用 GET /api/friends 拉好友列表时跟着一起带回来了。'
                  '这种安排省掉的是"每发一条消息额外发一次公钥请求"那笔开销，前端往返次数因此能减下来一截。')
    add_para(doc, '后端在更新公钥接口里只做算法白名单校验，'
                  '并不解析或验证公钥本身。'
                  '理由也不复杂：公钥本身没有保密属性，'
                  '真正棘手的是"怎么确认这把公钥真的来自对方"。'
                  '工业级方案靠指纹比对、可信目录或带外通道来解决这一点。'
                  '本系统目前的假设是双方信任服务端返回的公钥——'
                  '这是相对工业级 E2EE 的一项简化，明确留待后续工作处理。')

    add_heading(doc, '4.4.4  双重密文加密与服务端密文转发', level=3)
    add_para(doc, '用户按下发送的瞬间，前端并不会把明文直接提交上去。它走的是这样一段流程：先拿接收方的公钥跑一次 RSA-OAEP，'
                  '再用发送方自己的公钥又跑一次——同一段明文加密两遍，得到两份内容上等价、但只能由各自私钥才能解开的独立密文，'
                  '然后这两份一起通过 POST /api/messages 交给服务端。"双密文"这件事不是把流程故意做复杂，而是被需求挤出来的——'
                  '如果只留接收方那份能解的，发送方在自己设备上回看不了自己发过的内容，体验当场塌掉；'
                  '反过来如果让服务端再多存一份明文给双方查阅，端到端加密那条边界又破了。'
                  '让收发两边各持一份只能用本端私钥解开的副本，差不多就是在"历史得读到"和"服务端不能碰明文"两件事之间能找到的最稳的落点。'
                  '前端这段加密的关键代码如下：')
    add_code_block(doc, '''const receiverPublicKey = await importPublicKey(friend.publicKey)
const senderPublicKey = await importPublicKey(authStore.user.publicKey)
const content = draft.value.trim()
const receiverEncrypted = await encryptMessage(receiverPublicKey, content)
const senderEncrypted = await encryptMessage(senderPublicKey, content)
const { data } = await http.post('/messages', {
  receiverId: currentFriendId.value,
  senderCiphertext: senderEncrypted.ciphertext,
  senderAlgorithm: senderEncrypted.algorithm,
  receiverCiphertext: receiverEncrypted.ciphertext,
  receiverAlgorithm: receiverEncrypted.algorithm
})''')
    add_para(doc, '后端 MessageService 收到请求后，'
                  '先调用 FriendService.IsFriend 验证好友关系，'
                  '再检查两份密文非空、算法标识在 rsa-oaep-sha256 白名单内，'
                  '校验全部通过才写入 messages 表，'
                  '随后由 WebSocket Hub 推送给接收方。'
                  '从代码层面看，'
                  '后端没有调用任何解密接口，'
                  '也没有任何明文消息出现在内存或日志里——'
                  '这一点对端到端加密承诺至关重要。')
    add_para(doc, '"为同一条消息保存两份密文"这件事，说穿了是被现实卡出来的折中——本系统所有工程取舍里，这一处最关键。'
                  '假如只留接收方那一份，发送方换台设备或者刷新页面之后，自己发过的话就再也读不回来了，使用体验当场塌掉；'
                  '反过来如果让服务端留一份明文给双方备查，所谓"服务端碰不到明文"的承诺也就立刻作废。'
                  '双密文的安排算是这两难夹缝里的解法：每端拿到的副本，都只能被自己手上的私钥解开；加密强度没退让，'
                  '"双方都能回看自己的聊天历史"这件事也兜住了。')

    add_para(doc, '还有一个看起来不太起眼但其实挺关键的细节——Service 层的防御性校验。'
                  '为了避免后端在某种意想不到的边界情况下把明文写进数据库，本系统对密文字段做了相当严格的检查：'
                  '只要任一份密文是空的、或者请求里带的算法标识不在白名单里，就直接 400 拒掉，并把这一笔写进错误日志。'
                  '这道防线主要是为两种情况准备的：一是程序员自己改代码时手滑，二是有人在外面恶意构造请求。'
                  '加上这一层之后，messages 表里能落进去的就只有合法密文。')

    add_heading(doc, '4.4.5  接收端解密与历史消息展示', level=3)
    add_para(doc, '接收端做解密的时机其实只有两种：一种是 WebSocket 推过来新消息那一刻，另一种是用户主动通过 HTTP 把历史消息拉回来时。'
                  '两条路径走的解密逻辑被统一收到 selectMessagePayloadForUser 和 toRenderMessage 两个函数里——'
                  '前者按当前登录用户的 ID 判断需要解的是 sender_ciphertext 这一份还是 receiver_ciphertext 那一份，'
                  '后者再拿本地私钥去调 window.crypto.subtle.decrypt 把真正的解密动作完成。'
                  '解密顺利的话，明文就直接渲染出来；如果解密报错（最常见的就是用户换了设备，本地私钥跟服务端那一把公钥对不上），'
                  'UI 会把这条消息退化成 "***（已加密）" 这种占位文本，同时在错误提示区显示"当前设备没有可用私钥"，'
                  '把状况告诉用户，让他至少知道发生了什么。')
    add_para(doc, '要承认的是，'
                  '由于私钥只保留在最初生成它的那台设备上，'
                  '本系统暂不支持多设备解密同一份历史。'
                  '这是 RSA 简化方案的固有局限，'
                  '也是后续工作里一个明确方向。')

    add_heading(doc, '4.4.6  安全性分析', level=3)
    add_para(doc, '按端到端加密的基本判据来量这套实现，能对得上的部分如下：'
                  '消息明文从头到尾只出现在通信两端的浏览器里；服务端被剥得只剩三件事——把公钥存起来、把密文落库、把消息转发给目标连接。'
                  '换句话说，假设数据库被整库捞走，攻击者手上拿到的也无非是一堆 Base64 密文加上几把公钥，'
                  '没有对应私钥的话恢复不出任何聊天内容。这正是它和传统"TLS + 明文落库"方案之间最有意义的差别：'
                  '同样是服务端被攻破，原本会直接外泄的明文，现在退化成不可读的密文——后果的量级是不一样的。')
    add_para(doc, '不过这话也要说清楚：本系统目前的实现更接近'
                  '"前端本地加密 + 服务端密文转发与存储"，'
                  '离工业级 E2EE 体系还有距离。差在哪几处，下面分别说。')
    add_para(doc, '第一个缺口在公钥真实性校验。'
                  '用户拿到的对方公钥来自服务端——'
                  '一旦服务端被攻破，'
                  '它完全可以把对方公钥替换成攻击者的，'
                  '从而发起中间人攻击。'
                  'Signal 这类成熟方案靠指纹比对、可信目录或带外验证来堵这个洞，'
                  '本系统目前没做。')
    add_para(doc, '前向保密这块也欠着账。本系统里 RSA 私钥是长期凭证——它从生成那天起就一直用，'
                  '万一哪天泄露出去，过去存下来的密文从理论上讲都能被回过头去逐条解开。'
                  'Signal 把这种风险压得很低，路子是 Double Ratchet：每条消息都派生一把全新的临时会话密钥，'
                  '哪怕某一把被攻破，能殃及的也只有那一小段时间窗。这套能力，眼下的实现没有覆盖。')
    add_para(doc, '第三个缺口在会话密钥协商。'
                  '业界通行的做法是用 RSA/ECC 协商出短期会话密钥，'
                  '再用 AES-GCM 这种对称算法加密实际正文，'
                  '兼顾安全与性能。'
                  '本系统直接用 RSA-OAEP 加密单条短消息，'
                  '受 2048 位 RSA 自身的限制——'
                  '单次最多约 190 字节明文——'
                  '在 Web 端聊天文本场景下勉强够用，'
                  '但一旦要支持长文本或文件，'
                  '就必须切到"RSA 加密会话密钥 + AES-GCM 加密正文"的混合方案上。')
    add_para(doc, '多设备同步是用户最容易察觉的短板。私钥目前只对生成它的那个浏览器有效，换了电脑或者清了缓存以后，'
                  '过去的密文就再也解不回来。放到真实产品里这是个挺要命的问题，搁在毕业设计的尺度上还能接受——但不能假装它不存在。')
    add_para(doc, '把这些口子全部摊在台面上，并不是想替现有版本开脱。更想说清楚的是它现在停在哪个位置：'
                  'E2EE 的核心想法已经落进真正能跑的代码里了，但要再往工业级靠近，缺的几步都很明确——'
                  '沿着这套架构继续补密钥协商、对称加密和多设备能力，剩下的工作并不是没有路。')
    add_page_break(doc)
